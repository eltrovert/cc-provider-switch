# User-specific config (contains active provider)
config.json

# Claude Code local settings
.claude/

# Provider files may contain API tokens
# Copy from examples/ and customize
# (already-tracked safe providers are unaffected)
providers/*.json
